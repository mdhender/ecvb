# sqlite files
*.db
*.db-*
# the map and the starting kit, copied in from games/claude by replay.sh.
# Nine of the ten factions never move, so which map it is does not matter --
# the game is here to have something to submit an order file to. The tenth,
# faction 6, does move: it launches a crossing it cannot finish quickly so it
# can give orders to a ship that is nowhere, and for that it needs real
# stellia at real distances.
stellia-seed.json
systems-seed.json
planets-seed.json
deposits-seed.json
home-planet-seed.json
# what the parser answered. The findings themselves are written down in
# docs/plan/parser-burndown.md, which is the record worth keeping.
diagnostics/
